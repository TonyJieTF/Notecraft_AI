[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "backend/**,database/**,notecraft-backend-scaffold/**"
  SECRETS_SCAN_OMIT_KEYS = "DATABASE_URL"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
